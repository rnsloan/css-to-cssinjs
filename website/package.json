{
  "name": "website",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "parcel index.html -p 3000 --hmr-port 43210",
    "build": "parcel build index.html --public-url ./",
    "deploy": "yarn run build && node ./deploy.js"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "parcel-bundler": "^1.12.3"
  },
  "dependencies": {
    "css-to-cssinjs": "^1.3.4"
  }
}
